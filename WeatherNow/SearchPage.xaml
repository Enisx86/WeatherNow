<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:WeatherNow.ViewModels"
             xmlns:models="clr-namespace:WeatherNow.Models"
             xmlns:Controls="clr-namespace:WeatherNow.Controls"
             x:DataType="vm:SearchPageViewModel"
             x:Class="WeatherNow.SearchPage"
             Title="SearchPage">
    
    <!-- 
        i have no idea why the floating navbar still works, it's honestly black magic
        don't mess up the layouts though, bcuz .NET MAUI loves trolling and i can't figure this out again
    -->
    <Grid>
        <ScrollView>
            <VerticalStackLayout>
                <Grid ColumnDefinitions="2*,*" Margin="10">
                    <Entry Grid.Column="0" Text="{Binding SearchBarText, Mode=TwoWay}" Placeholder="Search here..."  />
                    <Button Grid.Column="1" Text="Search" Command="{Binding SearchCommand}" />
                </Grid>
                <ActivityIndicator IsRunning="{Binding IsSearching}" IsVisible="{Binding IsSearching}" />
                <VerticalStackLayout Padding="0,0,0,128" Spacing="24" BindableLayout.ItemsSource="{Binding AvailableCities}">
                    <BindableLayout.ItemTemplate>
                        <DataTemplate x:DataType="models:GeocodingResult">
                            <Grid ColumnDefinitions="*,8*,*">
                                <Border
                                    Grid.Column="1"
                                    StrokeShape="RoundRectangle 10"
                                    BackgroundColor="White"
                                    Padding="20">
                                    <Border.Shadow>
                                        <Shadow Brush="Black" Offset="2,2" Radius="5" Opacity="0.2" />
                                    </Border.Shadow>
                                    <Grid ColumnDefinitions="*,Auto">
                                        <VerticalStackLayout Grid.Column="0">
                                            <Label Text="{Binding name}" FontSize="20" />
                                            <Label Text="{Binding country}" FontSize="16" TextColor="#666" />
                                            <Label Text="{Binding CountryEmoji}" FontSize="24" />
                                        </VerticalStackLayout>
                                        <Label Grid.Column="1" Text="⟩" TextColor="#666" FontSize="20" VerticalOptions="Center" />
                                    </Grid>
                                </Border>
                            </Grid>
                        </DataTemplate>
                    </BindableLayout.ItemTemplate>
                </VerticalStackLayout>
            </VerticalStackLayout>
        </ScrollView>
        <Controls:FloatingNavbar ActiveTab="Search" />
    </Grid>
</ContentPage>